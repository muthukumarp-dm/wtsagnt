# Overnight status — 2026-05-17 (Sunday → Monday)

Tomorrow-morning brief, sorted by what you need to do first.

## ✅ What's verified working (no action needed)

1. **All 16 plan tasks implemented and committed.** 17 commits since the design lock at `7111c91`.
2. **All 58 tests pass.** Including the Supabase smoke (now that the migration is applied) and the new real-storage E2E.
3. **OpenAI key works** — `test_smoke_openai.py` passed against the real API.
4. **Supabase migration applied + connection works** — `projects/messages/generations` tables exist, `lesson-files` bucket exists.
5. **Full pipeline produces real files end-to-end with real OpenAI.** Three sets of real lesson decks + reckoners are in `prototype/outputs/`:
   - `20260516T204853Z_e2e_smoke/` — Grade 7 photosynthesis, 12 slides, 5 MCQs (local-storage variant)
   - `20260516T205006Z_e2e_revision/` — Grade 8 photosynthesis + cellular respiration (exercises the revision merger)
   - `20260517T025328Z_real_storage_smoke/` — Same flow but uploaded to + downloaded from real Supabase Storage with public signed URL (the strongest possible smoke without Twilio)

   **Open these before going live.** If quality looks good → ship. If not → iterate prompts.
   ```
   open prototype/outputs/20260517T025328Z_real_storage_smoke/lesson.pptx
   open prototype/outputs/20260517T025328Z_real_storage_smoke/reckoner.pdf
   ```
6. **Cost so far: ~$0.08** of OpenAI tokens across 3 full pipeline runs.
7. **Revision merger verified** — original ("grade 7 photosynthesis") + revision ("grade 8 + cellular respiration") → merger produced a coherent brief; downstream agents correctly produced "grade 8 on photosynthesis and cellular respiration".
8. **Real Supabase Storage upload + signed URL roundtrip verified** — `test_smoke_pipeline_real_storage.py` uploads → mints 7-day signed URL → httpx GET returns 200 with correct content → cleanup deletes test objects.
9. **Deployed to Railway.** Service `wtsagnt-monday` is live in workspace `muthukumarp-dm's Projects`:
   - **Public URL:** `https://wtsagnt-monday-production.up.railway.app`
   - **Service ID:** `a72d80f5-517b-41a9-b9c1-0456dff2689f`
   - **Project ID:** `2f96e94c-5e3c-49b6-bfde-0322c1d0fde8`
   - All env vars set in Railway dashboard, `PUBLIC_BASE_URL` set to the real domain
   - Uses railpack builder + Procfile (`web: uv run uvicorn src.server:app --host 0.0.0.0 --port $PORT`)
   - First deploy + redeploy both SUCCESS

## ⚠️ What's pending — your action required

### 1. ✅ ~~Apply the Supabase migration~~ — DONE (you did this overnight)

### 2. Rotate the OpenAI key (3 minutes — DO THIS FIRST)

The key currently in `prototype/.env` AND in Railway env vars is the one you pasted in chat — it's been in scrollback. Rotate at https://platform.openai.com/api-keys → revoke the old, create a new one → update **both**:
- `prototype/.env` (for local tests)
- Railway dashboard → wtsagnt-monday service → Variables → `OPENAI_API_KEY` (for deployed service)

Setting the var in Railway auto-triggers a redeploy.

### 3. ✅ ~~Set `PUBLIC_BASE_URL`~~ — DONE for Railway

Set to `https://wtsagnt-monday-production.up.railway.app` (the Railway public domain). You don't need to set this in `.env` unless you also want to run locally with ngrok; the deployed service has it as a Railway env var.

### 4. Join the Twilio sandbox from your demo phone

Twilio Console → Messaging → Try it out → WhatsApp → Sandbox settings. Note the `join <code>` phrase. Text that phrase from the demo phone to the sandbox WhatsApp number. **This takes ~30s but cannot be automated — Twilio requires the SMS to come from a real phone.**

### 5. Point Twilio's webhook at Railway

In the same Twilio sandbox settings, set **"WHEN A MESSAGE COMES IN"** to:

```
https://wtsagnt-monday-production.up.railway.app/webhooks/whatsapp
```

(POST method.) Save.

### 6. Send a test message from the joined sandbox phone

From your phone (joined per step 4), text the sandbox WhatsApp number:

> 30-min lesson for grade 7 on photosynthesis. Cover the process, inputs, outputs, where in the plant. 5 MCQs.

**Expected:**
- "Got it. Generating your lesson…" within 2–5 seconds (Railway cold start may add a few seconds on the first hit)
- Summary message within ~120s
- Reply `APPROVE` → two messages with `.pptx` and `.pdf` links within 5 seconds
- Tapping each link downloads the file (signed Supabase Storage URL, 7-day TTL)

### 7. ✅ ~~Railway deploy~~ — DONE

Service: `wtsagnt-monday` at `https://wtsagnt-monday-production.up.railway.app`.

To watch logs in real time during the demo:
```bash
cd prototype && railway logs --service wtsagnt-monday
```

To redeploy after env var changes (auto-triggered by var changes):
```bash
cd prototype && railway redeploy --service wtsagnt-monday
```

### 8. Sunday-evening dry-run + screen recording (Task 16)

The night before the Monday demo: do a complete dry-run on the deployed Railway URL, screen-record the happy path on your phone for ~30s, save to `prototype/demo/2026-05-17-dry-run.mp4`. **This is your insurance against Monday-morning flakiness** (Anthropic 503, Twilio sandbox hiccup, Railway cold start).

## Files of interest

- `docs/superpowers/specs/2026-05-17-monday-whatsapp-slice-design.md` — the design spec (architecture, CLAUDE.md compliance table, definition of done)
- `docs/superpowers/plans/2026-05-17-monday-whatsapp-slice.md` — the 16-task implementation plan
- `prototype/README.md` — setup + run + deploy instructions
- `prototype/outputs/2026*` — real .pptx + .pdf samples to inspect

## Commit history of this session

Run `git log --oneline` for the latest. 17+ commits since the design lock, including:
- Spec + plan + CLAUDE.md update (`7111c91`)
- Wave 1: scaffolding, schemas, prompts, pptx, pdf, settings, migration SQL
- Wave 2: state, reply_parser, whatsapp_adapter, storage_adapter (parallel subagents)
- Wave 3: pipeline (orchestration)
- Wave 4: FastAPI server
- README + NOTES + 2 E2E smoke tests (real OpenAI + real Supabase Storage)
- Procfile (for Railway railpack builder)

Not pushed to origin (per CLAUDE.md, only push when explicitly asked).
